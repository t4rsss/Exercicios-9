import math
import random

num = float(input("Digite um número para calcular a raiz quadrada: "))
raiz = math.sqrt(num)
print(f"Raiz quadrada de {num} é {raiz:.2f}")

aleatorio = random.randint(1, 100)
print(f"Número aleatório gerado: {aleatorio}")
